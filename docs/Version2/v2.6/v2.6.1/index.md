# Lumina v2.6.1 — RBAC & Multi-Tenancy

## Goal
Role-based access control for the Lumina cluster with namespace isolation.

## Deliverables
- User roles (admin, operator, viewer) with permission boundaries
- Namespace isolation: team A cannot see or modify team B's entities
- API authentication for cluster operations
