# Lumina v2.5.1 — Notification Adapters

## Goal
Deliver alerts to external communication channels.

## Deliverables
- Slack adapter (webhook-based)
- Email adapter (SMTP)
- SMS adapter (Twilio)
- Generic webhook adapter for any notification service

## Dependencies
- Existing `alert` action in the AST (already implemented)
