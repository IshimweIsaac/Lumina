-- main.lum

import "schema.lum"

let t = Thermostat { temp: 20, target: 22 }

rule "On"  { when t.status == "Heating" becomes true then show "Furnace: {t.status}" }
rule "Off" { when t.status == "Idle"    becomes true then show "Furnace: {t.status}" }

update t.temp to 25
update t.temp to 15
